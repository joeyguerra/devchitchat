apiVersion: apps/v1
kind: Deployment
metadata:
  name: devchitchat-web
spec:
  selector:
    matchLabels:
      app: devchitchat-web
  replicas: 1
  template:
    metadata:
      labels:
        app: devchitchat-web
    spec:
      containers:
      - name: devchitchat-web
        image: local/devchitchat-web:0.1.1
        imagePullPolicy: IfNotPresent
        env:
        - name: PORT
          value: "8080"
        - name: NODE_ENV
          value: "production"
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: devchitchat-web-service
spec:
  selector:
    app: devchitchat-web
  ports:
    - protocol: TCP
      port: 8080
